<table class="table">
        <% @users. each do |user| %>
            <thead>
                <tr>
                    <td><%= user.name %></td>
                </tr>
            <tbody>
                <tr>
                    <td><%= user.prefecture %>
                    <%= user.city_address %>
                    <%= user.building %></td>
                </tr>
            </tbody>
            <tfoot>
                <tr>
                    <td><%= user.email %></td>
                </tr>
            </tfoot>
        <% end %>
    </table>
    <div class="btn-group">
            <button><%= link_to "住所一覧", addresses_path, class="btn btn-default" %></button>
            <button><%= link_to "住所入力フォーム", new_address_path, class="btn btn-default" %></button>
            <button><%= link_to "ユーザー情報編集", edit_user_path(user.id), class="btn btn-default" %></button>
    </div>
        <p><%= link_to "退会する？", user_path(user.id), method: :delete %></p>

<h2>お気に入りリスト</h2>
        <% @favorite each do |favorite| %>
            <div class="container">
                <div class="col-md-12 col-md-1">
                </div>
                <div class="col-md-12 col-md-3">
                    <%= attachment_image_tag @favorites, :jacket_image_id, format: 'jpeg' size:"60*60" %>
                    <% end %>
                </div>
                <div class="col-md-12 col-md-5">
                    <p><%= favorite.item.title %></p>
                    <p><%= favorite.item.artist %></p>
                    <p><%= favorite.item.price %></p>
                    <% end %>
                </div>
                <div class="col-md-12 col-md-3">
                    <table class="table">
                        <tbody>
                            <tr>
                                <td><%= link_to "お気に入り", favorites_path(user.id), class="btn btn-default" %>
                            </tr>
                            <tr>
                                <td>
                                    <div class="btn-group">
                                        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                                            個数
                                        <span class="caret"></span>
                                        </button>
                                        <ul class="dropdown-menu" role="menu">
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">1</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">2</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">3</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">4</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">5</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">6</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">7</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">8</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">9</a></li>
                                            <li role="presentation"><a role="menuitem" tabindex="-1" href="#">10</a></li>
                                        </ul>
                                    </div>
                                </td>
                                <td>
                                    <%= link_to "カートに入れる", cart_items_path(user.id), class="btn btn-default" %>
                                </td>
                            </tr>
                        <tfoot>
                            <td><%= link_to 'いいね一覧', favorites_path(user.id) %></td>

<h2>購入履歴</h2>
    <%= @orders each do |order| %>
        <li><span class="caret"></span>
            <%= link_to orders.created_at %>
            <%= link_to '詳細へ', orders_path(user.id) %>
        </li>
            <table class ="table">
                <thead>
                    <tr>
                        <th>注文番号</th>
                        <th>商品</th>
                        <th>配送先</th>
                        <th>配送状況</th>
                        <th>レビューを書く</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><%= orders.id %></td>
                        <td><%= orders.items.title %></td>
                        <td><%= orders.prefecture+orders.city_address+orders.building %></td>
                        <td><%= orders.status %></td>
                        <td><%= link_to 'レビューを書く', item_path(item.id), class:"btn btn-default" %>
                    </tr>
                </tbody>
                <tfoot>
                    <td><%= link_to '購入履歴一覧', orders_path %>
                </tfoot>
            </table>
        <% end %>

<% @users. each |user| %>
    <td><%= user.name %></td>
    <td><%= user.prefecture %>
        <%= user.city_address %>
        <%= user.building %></td>
    <td><%= user.email %></td>
    <div class="button">
        <%= link_to "住所一覧", address_index_path %>
        <%= link_to "住所入力フォーム", address_edit_path %>
        <%= link_to "ユーザー情報編集", user_edit_path(user.id) %>
    </div>
    <%= link_to "退会する？, %>
