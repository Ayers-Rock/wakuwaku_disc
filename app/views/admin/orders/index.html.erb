<h1>受注履歴</h1>
<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
<br>

<table>
	<tr>
		<th>購入者</th>
		<th>購入日</th>
		<th>個数</th>
		<th>金額（税込）</th>
		<th>削除</th>

	</tr>
		<% @orders.each do |order| %>
		<tr>
			<td text-align:center;><font size="4"><%= order.user_id %></fond></td>
			<td text-align:center;><font size="4"><%= order.created_at %></font></td>
			<td text-align:center;><font size="4"><%= order.amount %></font></td>
            <td text-align:center;><font size="4"><%= order.total_price %>円</font></td>
			<td><%= link_to '注文詳細', admin_order_path(order.id) %></td>
		</tr>
		<% end %>

</table>
<br>
<span>　　</span><%= link_to '商品一覧へ戻る', items_path, :class => 'btn' %><span>