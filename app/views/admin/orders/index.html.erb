<h1>受注履歴</h1>
<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>
<br>

<table>
	<tr>
		<th>購入者</th>
		<th>購入日</th>
		<th>個数</th>
		<th>金額（税込）</th>
		<th>削除</th>

	</tr>
		<% @cart_items.each do |cart_item| %>
		<tr>
			<td text-align:center;><img height="80" src="<%=h cart_item.image_url %>"/></td>
			<td text-align:center;><font size="4"><%= cart_item. %></font></td>
			<td text-align:center;><font size="4"><%= cart_item.quantity %></font></td>
            <td text-align:center;><font size="4"><%= (cart_item.price).to_i %>円</font></td>
			<td><%= link_to '削除', cart_item_path(@cart_item.id), method: :delete %></td>
		</tr>
		<% end %>

</table>
<br>
<span>　　</span><%= link_to '商品一覧へ戻る', items_path, :class => 'btn' %><span>