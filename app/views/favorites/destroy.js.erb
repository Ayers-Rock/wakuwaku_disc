$("#favorite-button-<%= @item.id %>").html("<%= j(render partial: 'favorite', locals: {favorites: @favorites, items: @items, item: @item, favorite: @favorite, favorite_ranks: @favorite_ranks, monthly_ranks: @monthly_ranks, weekly_ranks: @weekly_ranks}) %>")