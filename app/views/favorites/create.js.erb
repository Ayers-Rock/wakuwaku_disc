$(".favorite-button-<%= @item.id %>").html("<%= j(render partial: 'favorite', locals: { item: @item, favorite_ranks: @favorite_ranks, monthly_ranks: @monthly_ranks, weekly_ranks: @weekly_ranks }) %>")