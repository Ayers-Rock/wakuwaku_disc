<div class="body">
    <div class="container">
        <div class="row">
            <div class="col-xs-10 col-xs-offset-1">
                <div class="messeage">
                    <h2>ご注文ありがとうございました。</h2>
                </div>
                <div class="box">
                    <table class="">
                        <tr>
                            <th>ご注文番号 <%= @order.order_number %></th>
                        </tr>
                        <tr>
                            <th>商品画像</th>
                            <th>タイトル</th>
                            <th>アーティスト</th>
                            <th>数量</th>
                            <th>小計</th>
                        </tr>
                        <% @order_items.each do |order_item| %>
                            <tr>
                                <td class="thanks-data"><%= attachment_image_tag order_item.item, :jacket_image, :fill, 60, 60, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: 'no_image.jpg', size: '100x100' %></td>
                                <td class="thanks-data"><%= order_item.item.title %></td>
                                <td class="thanks-data"><%= order_item.item.artist.artist_name %></td>
                                <td class="thanks-data"><%= order_item.amount %></td>
                                    <% subtotal = order_item.item.price * order_item.amount * 1.10 %>
                                <td class="thanks-data"><%= subtotal.round(0) %>円</td>
                            </tr>
                        <% end %>
                    </table>
                    <p class="thanks_postage">送料    500円</p>
                    <p class="thanks_total"><span>合計</span> 　<%= @order.total_price.floor %>円<span>(税込)</span> </p>
                </div>
            </div>
        </div>
    </div>
</div>





<div class="link_path">
    <%= link_to 'トップページへ戻る',root_path %>
</div>

