<h2>購入履歴</h2>

<% @orders.each do |order| %>
<table>
  <thead>
    <tr>
      <%= link_to '(詳細へ)', order_path(order.id) %>
    </tr>
    <tr>
        <th>商品</th>
        <th>配送状況</th>
        <th></th>
    </tr>
  </thead>
  <tbody>
    <% order.order_items.each do |order_item| %>
      <tr>
        <td><%= order_item.item.title %></td>
        <td><%= order_item.order.status %></td>
        <td><%= %></td>
          <% if order_item.order.status == "preparing" %>
          <%
=begin%>
 status == 0だとダメだった　databaseだと０で保存されてるけどviewに持ってくるときには文字列になってる・・？ 
<%
=end%>
            <td> </td>
          <% else %>
            <td>
              <%= link_to item_reviews_path(order_item.item_id) do %>
                <button class="btn-sticky">レビューを書く</button>
              <% end %>
            </td>
          <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
<% end %>