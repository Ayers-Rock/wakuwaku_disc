
<h1>購入履歴・配送状況詳細</h1>

<table>
    <thead>
        <th>商品</th>
        <th>単価（税込み）</th>
        <th>個数</th>
        <th>小計</th>
    </thead>

    <% @order_items.each do |order_item| %>
        <tbody>
            <td><%= order_item.item.title %></td>
            <td>¥<%= order_item.purchase_price %></td>
            <% tax_included = order_item.purchase_price * 1.10 %>
            <td><%= tax_included %></td>
            <td><%= order_item.amount %></td>
            <% subtotal = tax_included * order_item.amount %>
            <td>￥<%= subtotal %></td>
        </tbody>
    <% end %>

    <tfoot>
        <tr>
            <th></th>
            <th></th>
            <th> 送料</th>
            <th>　500円</th>
        </tr>
    </tfoot>
</table>
<P>*配送状況はあくまで目安です。</p>
<p>お支払い合計　　￥<%= @order.total_price %></p>

<p>注文者情報</p>
<table>
    <tbody>
        <tr>
            <th></th>
            <td></td>
        </tr>
    </tbody>
<table>

<p>配送先</p>
<p>[変更する]ボタンがない場合はすでに出荷準備に入ってるため配送先住所を変更できません。</p>
<table>
    <tbody>
        <tr>
            <th></th>
            <td></td>
        </tr>
    </tbody>

<table>

</table>


<p>お支払い方法</p>
<%= @order.payment %>



<%= link_to '購入履歴・配送状況一覧に戻る', orders_path %>